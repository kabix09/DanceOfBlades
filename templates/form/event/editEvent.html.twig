{% extends 'base.html.twig' %}

{% block title %}Edit event{% endblock %}

{% block body %}

    <div class="jumbotron d-flex align-items-center" style="height: 85%;">
        <div class="container" style="width: 25rem;">   <!--  padding: 15px; margin: auto; margin-top: 30px; -->

            <div class="card">
                <div class="card-body">
                    <h3 class="card-title text-center">Edit event</h3>
                    <p class="card-text">
                        {{ include('form/event/_form.html.twig', {
                            event: editEvent,
                            button_text: 'Update'
                        }) }}
                        {# action_path: path('app_edit_map', {'id': editMap.vars.data.id }), #}
                    </p>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
