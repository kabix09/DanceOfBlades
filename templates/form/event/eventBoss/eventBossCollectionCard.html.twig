<div class="row mt-2" style="width: 97%">
    <div class="col-10 pe-0">
        <div class="card">
            <h5 class="card-header">Boss</h5>
            <div class="card-body">
                <p class="card-text">
                    {{ form_label(boss.boss) }}
                    {{ form_widget(boss.boss) }}

                <div class="d-flex justify-content-between align-items-center">
                    {{ form_row(boss.create) }}
                    <div class="w-100"></div>
                    {{ form_row(boss.select) }}

                </div>

                    {{ form_row(boss.difficultnessLevel) }}
                    {{ form_row(boss.points) }}

                {# display remove & add buttons - added by js #}
                </p>
            </div>
        </div>
    </div>
</div>
