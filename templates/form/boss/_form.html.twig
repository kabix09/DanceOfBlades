{{ form_start(boss, {'method': "POST", 'action': path}) }}
    <div class="row">
        <div class="col-8" style="border-right: 2px solid #848484">
            {{ form_row(boss.name) }}
            {{ form_row(boss.level) }}
            {{ form_row(boss.description) }}

            <div class="d-flex justify-content-between align-items-center">
                {{ form_row(boss.strength) }}
                {{ form_row(boss.defence) }}
            </div>

            <div class="d-flex justify-content-between align-items-center">
                {{ form_row(boss.health) }}
                {{ form_row(boss.magic) }}
            </div>

            {{ form_row(boss.speed) }}
        </div>

        <div class="col-4">
            {{ form_row(boss.race) }}
        </div>
    </div>

    <div class="d-flex justify-content-end align-items-center pe-2">
        <button class="btn btn-primary btn-block" type="submit" style="margin-top: 10px;">
            {{ button_text }}
        </button>
    </div>

{{ form_end(boss) }}
